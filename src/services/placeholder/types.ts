// این فایل انواع تایپی سرویس placeholder را تعریف می‌کند.
export type CreatePlaceholderResult = {
  placeholder: string
  imageUrl: string
}
export type CreatePlaceholderQuery = {
  imageUrl: string
}
